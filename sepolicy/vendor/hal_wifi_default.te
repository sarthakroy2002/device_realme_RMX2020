allow hal_wifi_default self:capability sys_module;

# Get hotspot prop
get_prop(hal_wifi_default, mtk_wifi_hotspot_prop)

# Allow to set driver/fw version into property
set_prop(hal_wifi_default, vendor_wifi_version)
